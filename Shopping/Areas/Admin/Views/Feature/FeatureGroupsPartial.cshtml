@model IEnumerable<Shopping.Database.Model.FeatureGroup>
@{
    if (Model.Count()>0)
    {

            @foreach (var _group in Model)
            {
                //<a class="btn btn-warning me-1 m-1">@sections.SectionName</a>
            <h2 class="text-dark"><span class="bi bi-asterisk text-bg-dark"></span>@_group.GroupName</h2>

                @foreach (var _sections in _group.FeatureSections.Where(f => f.FeatureGroup.GroupName == _group.GroupName))
                {
                    //<a class="btn btn-danger">@item.Title</a>
                <h4 class="text-success"><span class="bi bi-braces-asterisk text-bg-success"></span>@_sections.SectionName</h4>
                }
            }




        // <table class="table">
        //     <thead>
        //         <tr>
        //             <th>
        //                 @Html.DisplayNameFor(model => model.GroupName)
        //             </th>
        //             <th></th>
        //         </tr>
        //     </thead>
        //     <tbody>
        //         @foreach (var item in Model)
        //         {
        //             <tr>
        //                 <td>
        //                     @Html.DisplayFor(modelItem => item.GroupName)
        //                 </td>
        //             </tr>
        //         }
        //     </tbody>
        // </table>

    }
    else
    {
        <h1 class="text-danger">فعلا ویژگی برای این قسمت ثبت نشده است</h1>
    }
}
